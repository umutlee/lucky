import 'package:flutter/material.dart';

/// 應用程序顏色主題
class AppColors {
  // 主要顏色
  static const primary = Color(0xFF6200EE);
  static const primaryVariant = Color(0xFF3700B3);
  static const secondary = Color(0xFF03DAC6);
  static const secondaryVariant = Color(0xFF018786);
  static const accent = Color(0xFF018786);

  // 背景顏色
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const error = Color(0xFFB00020);

  // 文字顏色
  static const onPrimary = Color(0xFFFFFFFF);
  static const onSecondary = Color(0xFF000000);
  static const onBackground = Color(0xFF000000);
  static const onSurface = Color(0xFF000000);
  static const onError = Color(0xFFFFFFFF);

  // 運勢相關顏色
  static const fortuneExcellent = Color(0xFF4CAF50);
  static const fortuneGood = Color(0xFF2196F3);
  static const fortuneNormal = Color(0xFFFFA726);
  static const fortuneBad = Color(0xFFF44336);

  // 漸變色
  static const gradientStart = Color(0xFF6200EE);
  static const gradientEnd = Color(0xFF03DAC6);

  // 分隔線和邊框
  static const divider = Color(0x1F000000);
  static const outline = Color(0x1F000000);

  // 陰影
  static const shadow = Color(0x1F000000);
} 